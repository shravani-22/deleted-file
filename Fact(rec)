#include<stdio.h>
int fact(int a)
{
    if(a==0)
   return 1;
      
    else
    return(a*fact(a-1));
}
void main()
{
    int f,n;
    printf("enter number");
    scanf("%d",&n);
    if(n<0)
    printf("invalid input");
    else
    f=fact(n);
    printf("fact of %d is %d",n,f);
    
}
